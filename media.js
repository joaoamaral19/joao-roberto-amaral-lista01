let numeros = [7, 8, 6, 9, 10];

let soma = 0;

for (let i = 0; i < numeros.length; i++) {
    soma = soma + numeros[i];
}
let media = soma / numeros.length;

console.log("A média dos valores é: " + media);
